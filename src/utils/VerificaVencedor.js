export default function verificarVencedor(quadrado, jogador){
    for (let i= 0; i< array.length; i++) {
        if (quadrado[i*3] === jogador 
            && quadrado[i * 3 + 1] === jogador
            && quadrado[i * 3 + 2] === jogador){
        }        
    }
}